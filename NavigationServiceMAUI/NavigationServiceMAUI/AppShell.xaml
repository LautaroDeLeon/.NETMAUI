<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NavigationServiceMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NavigationServiceMAUI.Views"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MovieListPage}"
        Route="AppLaunchPage" />-->

    <TabBar>
        <ShellContent
            Title="Peliculas"
            Icon="peliculas"
            ContentTemplate="{DataTemplate local:MovieListPage}"
            Route="MainPage" />
        <ShellContent
            Title="Actores"
            Icon="actor"
            ContentTemplate="{DataTemplate local:StudentListPage}"
            Route="MainPage1" />
        <ShellContent
            Title="Favoritos"
            Icon="star"
            ContentTemplate="{DataTemplate local:FavMovieListPage}"
            Route="MainPage2" />
       
    </TabBar>

</Shell>
